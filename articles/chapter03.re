= データの永続化

== 第2章までの課題：揮発的なデータ保持

第2章では、データを登録・取得・更新・削除できる最小限のデータベースを実装しました。
このデータベースでは、ユーザーが入力したデータはすべて@<b>{HashMap}に保存されます。

しかし、この実装には大きな課題があります。
それは、すべてのデータをメモリ上で管理しているため、プログラムを終了するとHashMapも同時に破棄され、データが消えてしまうという点です。

実際のデータベースでは、このようなことは起こりません。
例えば、顧客管理システムでデータを登録したあと、パソコンの電源を切ったからといってデータが消えてしまっては困ります。
また、ECサイトで登録された商品情報や注文履歴も、システムを停止するたびに失われることはありません。
このように、プログラムを終了したあともデータを保持し続ける性質を@<b>{永続化（Persistence）}といいます。
データベースにとって、永続化は最も重要な機能の一つです。
本章では、このデータベースの永続化に取り組みます。

== 本章の方針

「電源を切るとデータが消えてしまう」という課題を解決するために、本章ではメモリ上のデータをディスク（ファイル）に保存し、次回起動時に復元できる仕組みを実装します。

具体的には、以下の順序で実装を進めます。

 1. @<b>{ファイルを扱う準備}: データベースのデータを保存するためのファイルと、その保存先ディレクトリを準備する仕組みを作ります。
 2. @<b>{データの直列化（シリアライズ）設計}: メモリ上のデータ構造（HashMap）を、ファイルに書き込めるテキスト形式へと変換するルールを決めます。
 3. @<b>{ファイルへの保存と復元の実装}: CRUD操作でデータが変更されるたびにファイルへ書き出し、プログラム起動時にはファイルからデータを読み込んで復元する処理を組み込みます。

このステップを踏むことで、プログラムを再起動しても前回のデータが残っているという、データベースとしての必須条件を満たすことができます。まずは、データを保存するファイルの準備から見ていきましょう。

== ファイルを扱う準備

前節では、データを永続化する必要性について説明しました。
では、実際にはどこへデータを保存すればよいのでしょうか。
本節では、最も単純な方法として、データをテキストファイルへ保存します。
プログラムを終了してもファイルはディスク上に残るため、次回起動時に読み込むことでデータベースを復元できます。

まずは、データベースを保存するファイルを決めます。
本書では、プロジェクト内のdataディレクトリにデータベースファイルを配置します。

//emlist{
project
├── src
└── data
     └── chapter03.db
//}

プログラムでは、保存先を定数として定義しています。

//emlist{
private static final String DATA_PATH = "data/chapter03.db";
//}

Javaではファイルを扱う方法がいくつかありますが、本書ではJava NIOが提供するPathクラスを利用します。
以下のように、文字列として定義したパスからPathオブジェクトを生成し、ファイルの読み込みや書き込みもこれを利用して行います。

//emlist{
dataPath = Path.of(DATA_PATH);
//}

ファイルへ保存するためには、保存先のディレクトリが存在している必要があります。
しかし、初めてプログラムを実行したときは、dataディレクトリが存在しない場合があります。
そこで、まず保存先ディレクトリの有無を確認します。

//emlist{
if (!Files.exists(dataPath.getParent())) {
    Files.createDirectories(dataPath.getParent());
} else {
    loadFromFile();
}
//}

これにより、保存先ディレクトリが存在しなければ自動的に作成され、ユーザーが事前にdataディレクトリを作成していなくても、プログラムが必要なディレクトリを準備できるようになります。
一方で、保存先ディレクトリがすでに存在している場合は、以前に保存されたデータがある可能性があるため、loadFromFile()を呼び出し、保存されているデータを読み込みます。

ここまでで、データを保存するための準備が整いました。

== データの直列化（シリアライズ）設計

前節では、データベースを保存するためのファイルを用意しました。
しかし、ファイルを用意しただけではデータを保存できません。
データを保存するためには、@<b>{どのような形式でデータを書き込むか}を決める必要があります。
このように、メモリ上のデータ構造をファイルへ保存したり、ネットワークで送信したりできる形式（文字列やバイト列）に変換することを、@<b>{直列化（シリアライズ）}と呼びます。
本節では、この直列化の形式を決めていきます。

現在のデータベースでは、レコードはHashMap<Integer, String>に保存されています。
例えば、次のようなデータが登録されているとします。

//emlist{
| ID | Name  |
| -- | ----- |
| 1  | Alice |
| 2  | Bob   |
| 3  | Carol |
//}

メモリ上では、

//emlist{
1 → Alice
2 → Bob
3 → Carol
//}

という状態になっています。
この内容をそのままの状態でファイルへ保存することはできません。
そこで、人間にも読みやすいテキスト形式へ変換（直列化）して保存します。

本節では、1レコードを@<code>{キー,値}という形式で保存します。
例えば、@<code>{1,Alice}は、@<code>{キー = 1, 値 = Alice}という意味になります。
複数のレコードがある場合は、以下のように1レコードを1行として保存します。

//emlist{
1,Alice
2,Bob
3,Carol
//}

これが、本章で利用するデータベースファイルの内容です。
CSV（Comma-Separated Values）のように項目をカンマで区切り、1行を1レコードとして表しています。

== ファイルへの保存

前節では、データベースファイルの保存形式として、@<code>{キー,値}のテキスト形式を採用しました。
しかし、保存形式を決めただけでは、データベースの内容はファイルへ反映されません。
データベースへレコードを追加・更新・削除したときに、その内容をファイルへ書き出す処理が必要になります。
また、データベースを起動したときに、ファイルに保存されたデータベースの内容を読み込む処理も必要です。
本節では、その役割を担うsaveToFile()とloadFromFile()メソッドを実装します。

==== 保存と読み込みの実装

saveToFile()では、まずHashMapの全要素について、各要素を@<code>{キー,値}形式の文字列へ変換します。
そして、それらをリストにまとめ、ファイルへ書き出します。
この実装では、ファイル内に各レコードの位置やサイズを管理する仕組みがないため、特定の1件のレコードだけを直接上書きすることはできません。
そのため、1件のレコードを更新または削除した場合でも、変更後のHashMap全体を再度ファイルへ書き出し、ファイル全体を上書きする必要があります。

//emlist{
/** すべてのレコードを key,value 形式のテキストでファイルに保存する */
private void saveToFile() {
    try {
        List<String> lines =
            db.entrySet().stream()
              .map(entry -> entry.getKey() + "," + entry.getValue())
              .toList();
        Files.write(dataPath, lines);
    } catch (IOException e) {
        System.out.println("Failed to save database.");
    }
}
//}

起動時（読み込み）には、逆の処理を行います。
まず、ファイルを1行ずつ読み込み、各行をカンマで分割します。
キーは文字列のままでは利用できないため、整数に変換してHashMapに追加します。不正なフォーマットの行が混ざっていた場合は例外をキャッチし、スキップすることでエラーによる強制終了を防ぎます。
これにより、前回終了時と同じ状態のデータベースを安全に復元できます。

//emlist{
/** ファイルから key,value 形式のテキストを読み込んでレコードを復元する */
private void loadFromFile() {
    List<String> lines;
    try {
        lines = Files.readAllLines(dataPath);
    } catch (IOException e) {
        System.out.println("Failed to load database.");
        return;
    }

    for (String line : lines) {
        if (line.isBlank()) {
            continue;
        }
        try {
            String[] parts = line.split(",");
            db.put(Integer.parseInt(parts[0]), parts[1]);
        } catch (ArrayIndexOutOfBoundsException | NumberFormatException e) {
            System.out.println("Skipped invalid record format: " + line);
        }
    }

    System.out.println("Loaded records from file.");
}
//}

==== CRUD操作と保存処理

データベースの内容が変化するのは、

 * insert
 * update
 * delete

の3つの操作です。
そのため、それぞれのメソッドの最後でsaveToFile()を呼び出します。
例えば、insert()では、

//emlist{
db.putIfAbsent(id, value);
saveToFile();
//}

という流れになっています。
一方、selectはデータを参照するだけで内容を変更しないため、保存処理は不要です。

本書では、データを変更するたびにファイルへ保存しています。
このようにすることで、プログラムが途中で終了しても、直前までのデータが確実にディスクへ保存されています。

一方、実際のデータベースシステムでは、更新のたびにデータファイルへ直接書き込むとディスクI/Oが頻発し、処理性能が低下する可能性があります。そのため、更新対象のデータを一度メモリ上のバッファに保持して変更し、変更されたデータページを適切なタイミングでディスクへまとめて書き戻す仕組みが一般的に用いられます。
さらに、障害発生時にもデータの整合性を保つため、変更内容は通常、データページを書き戻す前にトランザクションログ（WAL）へ記録されます。これにより、システム障害後でもログを用いて未反映の更新を再実行したり、途中の更新を取り消したりすることができます。

本書では、永続化の基本的な考え方と処理の流れを理解しやすくするため、こうしたバッファ管理やログによる復旧機構は扱わず、更新のたびにファイル全体へ保存するシンプルな方式を採用しています。

== 永続化の確認

前節までで、データベースへ永続化の仕組みを追加しました。
これにより、データはHashMapに保存されるだけでなく、ディスク上のファイルにも保存されるようになりました。
本節では、実際にデータベースを操作し、プログラムを再起動したあともデータが保持されていることを確認します。

まずはデータベースを起動し、いくつかのレコードを登録します。

//emlist{
db > insert 1 Alice
db > insert 2 Bob
//}

この時点で、メモリ上のHashMapだけでなく、データベースファイルにも同じ内容が保存されています。
保存されたファイルをテキストエディタで開くと、次のような内容になっています。

//emlist{
1,Alice
2,Bob
//}

1行が1レコードとなっており、@<code>{キー,値}という形式で保存されていることが確認できます。
プログラム内部ではHashMapとして管理されていますが、ファイル上ではシンプルなテキスト形式へ変換されていることが分かります。

次に、データベースを終了します。

//emlist{
db > exit
//}

ここでプログラムは終了しますが、ファイルはディスク上に残っています。
その後、再びデータベースを起動します。
起動後に、

//emlist{
db > select
//}

を実行すると、

//emlist{
(1,Alice)
(2,Bob)
//}

と表示されます。
これは、第1章のときとは異なり、前回終了時のデータが正しく復元されたことを意味しています。

== まとめと次章への課題

本章では、第2章で作成したメモリ上のデータベースに、@<b>{永続化}の仕組みを追加しました。
第2章では、データはHashMapのみに保存されていたため、プログラムを終了するとすべて失われていました。
そこで本章では、この問題を解決するためにファイルを利用してデータを保存・復元する仕組みを実装しました。
この構成によって、データベースを終了しても、登録したデータを保持できるようになりました。

一方で、第3章の実装には新たな問題があります。
現在のsaveToFile()では、データが1件だけ変更された場合でも、データベースファイル全体を書き直しています。
例えば、100万件のレコードが保存されている状態で、

//emlist{
db > update 100 Robert
//}

を実行したとき、変更したいデータは1件だけです。
しかし、実際には100万件すべてを文字列へ変換し、ファイル全体を書き換えています。

1件だけではなくファイル全体を書き換えなければならないのには、理由があります。
ファイルは基本的に先頭から順番に読み書きを行うものであり、ファイル内に各レコードの位置やサイズを管理する仕組みがありません。そのため、テキスト形式でデータを保存すると、途中の1件だけをピンポイントで書き換えることが極めて難しくなるのです。

例えば、

//emlist{
1,Alice
2,Bob
3,Carol
//}

というファイルがあったとします。
これは、ファイル上では次のように改行文字を含めた連続した文字列（バイトの並び）として保存されています。

//emlist{
1,Alice\n2,Bob\n3,Carol\n
//}

ここで、@<code>{2,Bob} を @<code>{2,Robert} へ変更したい場合、文字数（データ長）が増えてしまいます。そのまま上書きすると、後続の @<code>{3,Carol} のデータ位置が後ろにズレてしまい、データが破損します。
その結果、ズレた以降のすべてのデータを書き直す、つまり「ファイル全体を書き直す」必要が生じてしまうのです。

データ量が少ないうちは問題ありませんが、レコード数が増えるにつれて、更新処理にかかる時間も非現実的なほど長くなってしまいます。
実際のデータベースでは、このような非効率な方法は採用されていません。変更があった部分だけを書き換えられるように、データを一定サイズの単位で管理しています。

第4章では、この問題を解決するために、データを@<b>{ページ（Page）}という固定サイズの単位で管理する仕組みを導入します。
ページ単位で管理することで、

 * 必要な部分だけを読み込める
 * 必要な部分だけを書き換えられる
 * データ量が増えても効率よく更新できる

という、本格的なデータベースとしての利点が得られます。
