= データベースとは何か

== データベースの役割

現代のソフトウェア開発において、データベースはなくてはならない存在です。Webアプリケーション、スマートフォンアプリ、企業の基幹システムに至るまで、ありとあらゆるシステムがデータベースを利用しています。

==== データベースが担う役割
データベースの最も根源的な役割は、@<b>{「データを安全に保管し、必要なときに高速に取り出せるようにすること」}です。
アプリケーションの裏側には常にデータベースが鎮座しており、ユーザーのプロフィール、商品の在庫、日々の決済履歴など、システムにとって最も価値のある「状態」を記憶し続けています。

==== データを管理するだけでは十分ではない
しかし、ただ単にデータを「保存する」だけであれば、データベースという大掛かりな仕組みは必要ないように思えます。
実際のシステム運用においては、データが増え続ける中で「数千万件のデータから、特定の1件を1ミリ秒で探し出す」といった圧倒的なパフォーマンスが求められます。さらに、「様々な条件を組み合わせて柔軟にデータを抽出する」「データを規則正しい構造（テーブル）として矛盾なく管理する」といった、高度なデータ処理能力が必要になります。

==== データベースが提供する機能
これらの厳しい要求に応えるため、リレーショナルデータベース（RDBMS）は以下のような強力な機能を提供しています。

 * @<b>{検索の高速化（インデックス）:} 膨大なデータの中から、目的のデータを一瞬で探し出す機能。
 * @<b>{効率的なデータ配置:} ディスクへの読み書き（I/O）回数を極限まで減らし、大容量のデータを高速に処理する機能。
 * @<b>{構造化されたデータ管理:} データを「テーブル」と「カラム」という形式で整理し、型（数値や文字列など）を厳格に管理する機能。
 * @<b>{柔軟な問い合わせ（SQL）:} 「20代で、かつ先月商品を購入したユーザー」のような複雑な条件を、人間が直感的に記述できるインターフェース。

私たちが普段何気なく使っているデータベースは、単なるデータの入れ物ではなく、これらの「高速性」と「利便性」を高度なアルゴリズムによって両立させているのです。

== ファイルとの違い

データベースについて学ぶ際、最初にぶつかる疑問があります。
「データを保存するだけなら、OSが提供している『ファイル（テキストやCSV）』に書き込めば十分ではないか？」という疑問です。

==== ファイルは「保存場所」である
確かに、数件から数千件程度の個人的なデータであれば、プログラムから直接ファイルを開いて追記・読み込みを行うだけで事足ります。ファイルシステムは、OSが提供する立派な「データの保存場所」です。

==== データベースはデータを管理する
しかし、「単なるファイル」はデータを物理的に配置する場所を提供しているだけで、その中身の整理や検索効率の面倒までは見てくれません。ファイルの中身をどう読み解き、どう検索するかは、すべてアプリケーション側（プログラマ）の責任になります。

==== データ量が増えると違いはさらに大きくなる
データ量が数百万件規模になり、要件が複雑化し始めると、単純なファイルベースの管理では以下のような致命的な問題が噴出します。

 * @<b>{検索が遅い:} ファイルの後半にあるデータを探すために、毎回ファイルの先頭からすべてを読み込んで確認しなければなりません（線形探索）。
 * @<b>{更新が非効率:} ファイルの途中にある数文字のデータを書き換えるだけでも、最悪の場合ファイル全体を再作成しなければならないことがあります。
 * @<b>{複雑な条件で探せない:} ファイルから「年齢が20歳以上で、名前に'A'が含まれる人」を探すには、そのためのプログラムを毎回イチから書かなければなりません。

データベースシステム（DBMS）とは、言わば@<b>{「生身のファイルシステムとアプリケーションの間に入り、データの配置から検索処理までを極限まで最適化してくれる仲介役」}なのです。

== なぜ自作するのか

==== データベースはブラックボックスになりやすい
このように強力で便利なデータベースですが、アプリケーション開発者にとって、その内部はしばしば「ブラックボックス」になりがちです。
SQLという呪文（クエリ）を投げると、データベースは裏側で良きに計らい、魔法のように高速にデータを返してくれます。しかし、「なぜインデックスを張ると速くなるのか」「内部でデータはどのような形でディスクに保存されているのか」を深く理解する機会は多くありません。

==== 既存の学習手法の限界
データベースの仕組みを学ぼうとして、既存の技術書を手に取ったことがあるかもしれません。しかし、従来の自作データベース系の書籍や資料には、以下のような課題がありました。

 * @<b>{理論が先行して難しすぎる:} B-Treeやリレーショナル代数などの学術的な理論ばかりが続き、コードを書く前に挫折してしまう。
 * @<b>{完成形のコードを見せられるだけ:} 「データベースとはこういうものだ」と、最初から複雑なアーキテクチャの完成形を提示され、コードの解説だけが進んでいく。「なぜそのコード（設計）が必要になったのか」という背景が分からない。
 * @<b>{巨大すぎるソースコード:} PostgreSQLやMySQLなどの実際のソースコードを読もうとしても、数百万行に及ぶC言語の海に溺れてしまう。

==== 本書で目指すこと：「課題駆動型」での開発
本書では、これら既存のアプローチとは全く異なる方法をとります。
それは、@<b>{「課題駆動型（Problem-Driven）」で、最小のコードから少しずつデータベースを育てていく}という手法です。

最初から完璧なアーキテクチャを目指すことはしません。「なぜこの複雑なデータ構造が必要なのか」「なぜSQLという言語が必要なのか」を頭ではなく「体験」として理解するために、あえて最初は「ダメな実装」からスタートします。

== 本書の進め方と全体像

==== 「問題」と「改善」を繰り返す
本書のすべての章は、以下のサイクルで進んでいきます。

 1. @<b>{最小限の実装:} とりあえず動くシンプルな仕組みを作る。
 2. @<b>{課題の発見:} データが増えると遅い、消えてしまうなどの「壁（問題）」にぶつかる。
 3. @<b>{理論の学習:} その問題を解決するために、先人たちが生み出したデータ構造やアルゴリズム（解決策）を学ぶ。
 4. @<b>{実装と改善:} 実際にコードを書き換えて、劇的にパフォーマンスが向上する（課題を解決する）快感を味わう。

この「課題にぶつかってから、解決策を実装する」というプロセスを経ることで、丸暗記ではない、生きた知識としてデータベースの仕組みを血肉にすることができます。

==== 本書で作るデータベースのロードマップ
本書では、以下のようなステップで本格的なデータベースを作り上げていきます。

 * @<b>{第2章 最小のデータベース:} まずは対話型のプログラム（REPL）を作り、メモリ上だけで動く最小のデータベースを作ります。しかし、「電源を切るとデータが消えてしまう」という課題に直面します。
 * @<b>{第3章 データの永続化:} データをファイルに書き込んで永続化します。しかし今度は、「データを更新するたびにファイル全体を書き直さなければならない」という非効率さに気づきます。
 * @<b>{第4章 ページ（Page）によるデータ配置:} 固定長の「ページ」という概念を導入し、ディスクの読み書きを効率化します。しかし、データが数万件になると「検索が遅すぎる（線形探索）」というパフォーマンス問題に直面します。
 * @<b>{第5章 B-Treeによる検索の高速化:} 検索を劇的に高速化するデータ構造「B-Tree」を導入します。内部の処理は完璧に近づきますが、今度は「独自のコマンドでは操作が複雑すぎる」というインターフェースの課題が出てきます。
 * @<b>{第6章 テーブル定義とSQLの実行:} 人間が直感的に操作できるように、SQLの構文解析（パーサ）を実装します。これにより、ついに「リレーショナルデータベース」らしい姿を手に入れます。
 * @<b>{第7章 インデックスの適用と実行計画:} 第5章で作ったB-TreeをSQLから利用できるようにします。さらに、クエリの条件を見て「インデックスを使うべきか、全件走査（フルスキャン）すべきか」を選択する、実行計画（プランナー）の基礎を実装します。
 * @<b>{第8章 構造の分離:} これまで追加してきた機能が複雑に絡み合ってきたため、「構造の分離」を行います。システムを適切な層（レイヤー）に整理し直すことで、拡張性と保守性の高いアーキテクチャを完成させます。
 * @<b>{第9章 振り返りとさらなる発展:} これまでに構築したデータベースのアーキテクチャを振り返り、本物の商用データベースとの違いや、今後の学習の道標を示します。

==== データベース全体の構成

小さなプログラムからスタートしますが、最終的に本書で実装するデータベースは、大きく分けると次の3つの役割（層）で構成されるアーキテクチャへと進化します。

//emlist{
               ユーザー
                  │
                  ▼
          SQL・コマンド入力
                  │
                  ▼
      ┌────────────┐
      │ フロントエンド層     │
      │ ・入力受付          │
      │ ・SQL解析           │
      └────────────┘
                  │
                  ▼
      ┌────────────┐
      │ 実行処理層           │
      │ ・実行計画の決定      │
      │ ・検索・更新処理      │
      └────────────┘
                  │
                  ▼
      ┌────────────┐
      │ ストレージ層         │
      │ ・ページ管理         │
      │  ・インデックス       │
      │ ・ファイル管理       │
      └────────────┘
                  │
                  ▼
          データベースファイル
//}

各層が役割を分担することで、機能を追加しやすく、保守しやすい構成になります。最初はストレージ層の基礎づくりから始め、徐々に上の層へと実装を広げていきます。

==== 本書を通して学べること

本書を読み終える頃には、データベースを利用するだけでは見えにくい、内部の仕組みを深く理解できるようになります。例えば、

 * データはディスク上にどのように保存されるのか
 * なぜデータベースは数千万件のデータからでも高速に検索できるのか
 * SQLという文字列はどのように解析されるのか
 * 実行方法（クエリプラン）はどのように決定されるのか
 * データベース内部はどのような層構造になっているのか

といった疑問に、自分で実装したコードを通して答えられるようになることを目指します。
本書で扱う実装は教育用に簡略化していますが、データベースの基本的な考え方は、実際の商用データベース管理システムにも共通しています。

完成形を目指すのではなく、@<b>{「進化の過程（変化）」を楽しむこと}。それが本書の最大のテーマです。
それでは次章から、いよいよキーボードを叩き、私たちだけのデータベースを作り始めましょう。
