= ページ（Page）によるデータ配置の改善

== 第3章までの課題：ファイル全体の書き直し

第2章では、プログラムを終了してもデータが消えないように、レコードをファイルへ保存する仕組みを作りました。データはメモリ上の @<code>{Map<Integer, String>} で管理し、起動時にファイルを読み込んで復元、insert/update/deleteによりレコードを1件でも変更したら @<code>{saveToFile} ですべてのレコードをテキスト形式でファイルへ書き直す、という実装です。

//emlist[第2章の保存処理：全レコードをファイルに書き直す（一部省略）][java]{
private void saveToFile() {
    List<String> lines =
        db.entrySet().stream()
          .map(entry -> entry.getKey() + "," + entry.getValue())
          .toList();
    Files.write(dataPath, lines);
}
//}

この実装では@<b>{1件のレコードを変更するだけでも、ファイル全体を書き直している}という点に無駄があります。10000件のレコードがある状態で1件を更新すると、変更のなかった9999件も含めてすべてを書き出すことになります。書き込む量はレコードの総数に比例して増えていくため、データが増えるほど1回の更新が重くなっていきます。

この無駄が生まれる根本の原因は、@<b>{保存の単位がファイル全体になっている}ことにあります。データベースが扱うデータは、ファイルの先頭から末尾まで1本のテキストとして並んでいるだけなので、途中の1件だけを差し替える手段がありません。

@<b>{ブロック}という単位でまとめて入出力を行っています。1バイトだけを書き換えたつもりでも、そのバイトを含むブロック全体が読み書きされます。したがって、データベースの側もこのブロックに合わせた単位でデータを配置し、読み書きすれば、ディスクとの入出力を無駄なく行えます。

本章では、この「ブロックに合わせた単位」を@<b>{ページ}として導入し、更新のたびにファイル全体を書き直すのではなく、@<b>{変更のあった部分だけを読み書きする}実装へと作り替えていきます。

== ページという単位の導入

ページとは、ファイルを一定の大きさで区切った固定長のブロックのことです。本章のプログラムでは、1ページを4096バイト（4KB）とします。この値はディスクやOSが扱うブロックの大きさに合わせたもので、多くの環境で自然な単位になります。

ファイル全体を1本のデータとして扱う代わりに、先頭から4096バイトずつ「ページ0」「ページ1」「ページ2」……と区切って管理します。こうしておくと、ファイルの先頭からのバイト位置は「ページ番号 × 4096」で計算できるため、@<code>{RandomAccessFile} の @<code>{seek} を使って目的のページへ直接移動し、そのページだけを読み書きできます。

//emlist[ページに関する定数][java]{
// ページサイズとスロットの定数
private static final int PAGE_SIZE = 4096; // 4KB
private static final int SLOT_SIZE = 64;
private static final int MAX_SLOTS = PAGE_SIZE / SLOT_SIZE; // 64
//}

ファイルは第2章のテキスト形式ではなく、バイト列を直接扱う @<code>{RandomAccessFile} として読み書きモードで開きます。ファイルの現在の大きさが分かれば、そこに何ページ分のデータが入っているかは次のように求められます。

//emlist[ファイルサイズからページ数を求める][java]{
int numPages = (int) Math.ceil((double) file.length() / PAGE_SIZE);
//}

これで、ファイルを「ページの集まり」として捉える準備ができました。次は、1つのページの中にレコードをどう並べるかを決めます。

== 固定長スロットによるレコードの設計

1ページ（4096バイト）の中を、さらに64バイトずつの区画に区切ります。この1区画を@<b>{スロット}と呼び、1つのスロットに1件のレコードを格納します。1ページは @<code>{4096 / 64 = 64} 個のスロットを持ちます。

//cmd{
ページ0 (4096バイト)
+----------+----------+----------+ ... +----------+
| スロット0 | スロット1 | スロット2 |     |スロット63 |
| 64バイト  | 64バイト  | 64バイト  |     | 64バイト  |
+----------+----------+----------+ ... +----------+
//}

スロットを@<b>{固定長}にしているのには理由があります。1件ごとの長さがまちまちだと、n番目のレコードがファイルのどこから始まるのかを知るために、先頭から順にレコードの区切りを数えていく必要があります。スロットの大きさを64バイトに固定しておけば、@<b>{s番目のスロットの位置は「s × 64」で即座に計算できる}ため、区切りを探す処理が一切いりません。

その代わり、レコードの実体が64バイトに満たなくても1スロットを丸ごと消費し、値の長さにも上限が生まれます。ここでは扱いやすさを優先し、この固定長の制約を受け入れます。

スロットの64バイトの内訳は次のとおりです。

//table[slot][スロット（64バイト）の内訳]{
オフセット	サイズ	内容
-----------------------------------------------------------------
0	1バイト	有効フラグ（1 = 使用中、0 = 空き）
1	4バイト	キー（int、ユーザーID）
5	4バイト	値の長さ（int、バイト数）
9	55バイト	値の本体（UTF-8、最大55バイト）
//}

先頭の1バイトは@<b>{有効フラグ}です。そのスロットが使用中か空きかを表し、レコードを読み書きするときはまずこのフラグを確認します。続く4バイトにキー（整数）、次の4バイトに値のバイト数、残りの55バイトに値の本体を格納します。@<code>{1 + 4 + 4 + 55} でちょうど64バイトになります。

Javaでこのバイト列を組み立てるには @<code>{ByteBuffer} を使います。ページを表すバイト配列の中から、対象スロットの位置（@<code>{slot × SLOT_SIZE}）を先頭として @<code>{ByteBuffer} を用意し、フラグ・キー・長さ・本体の順に書き込みます。

//emlist[スロットへ1件のレコードを書き込む][java]{
int offset = targetSlot * SLOT_SIZE;
ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
bb.put((byte) 1); // 有効フラグをセット
bb.putInt(id); // キーをセット

byte[] valBytes = value.getBytes(StandardCharsets.UTF_8);
int len = Math.min(valBytes.length, 55); // 最大55バイトに制限
bb.putInt(len);
bb.put(valBytes, 0, len);
//}

値は @<code>{Math.min(valBytes.length, 55)} で最大55バイトに切り詰めています。これは、値の本体に割り当てられた領域が55バイトだからです。読み出すときは、保存しておいた「値の長さ」の分だけバイトを取り出して文字列に戻します。

== ページ単位での読み書きの実装

スロットの設計が決まったので、CRUD（挿入・検索・更新・削除）をページ単位の読み書きで実装していきます。4つのコマンドはどれも、@<b>{ページを読み込む → スロットを調べる（または書き換える）→ 必要ならページを書き戻す}という共通の流れで動きます。ここでの要点は、書き込みが必要なときでも、ファイルへ書き戻すのは変更のあった1ページだけだという点です。

以降、コマンドごとに処理の流れを順を追って見ていきます。

=== 挿入（insert）

insert は、新しいレコードをファイルに1件書き加えるコマンドです。処理は次の手順で進みます。

 1. 現在のファイルサイズから、ページ数を求める。
 2. すべてのページ・スロットを調べ、同じキーがすでに存在しないか確認し、最初に見つかった空きスロットの位置を覚えておく。
 3. 空きスロットが1つも無ければ、新しいページを1枚増やしてその先頭スロットを使う。
 4. 書き込み先のページを読み込み、スロットにレコードを書き込む。
 5. そのページだけをファイルへ書き戻す。

1〜3が、書き込み先を決める部分です。ページを1枚ずつ読み込みながら、各スロットの先頭バイト（有効フラグ）を見て「使用中」か「空き」かを判断します。使用中スロットならキーを取り出して重複していないか確かめ、空きスロットなら（まだ候補を記録していなければ）その位置を書き込み候補として覚えます。

//emlist[重複チェックと空きスロットの探索][java]{
int numPages = (int) Math.ceil((double) file.length() / PAGE_SIZE);

int targetPage = -1;
int targetSlot = -1;

for (int p = 0; p < numPages; p++) {
  byte[] buf = new byte[PAGE_SIZE];
  file.seek((long) p * PAGE_SIZE);
  file.read(buf);

  for (int s = 0; s < MAX_SLOTS; s++) {
    int offset = s * SLOT_SIZE;
    if (buf[offset] == 1) { // 使用中スロット
      ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
      bb.get(); // 有効フラグをスキップ
      if (bb.getInt() == id) {
        System.out.println("Key already exists. Use update command to modify.");
        return;
      }
    } else if (buf[offset] == 0 && targetPage == -1) { // 最初の空きスロットを記録
      targetPage = p;
      targetSlot = s;
    }
  }
}

// 空きがない場合は新しいページを用意する
if (targetPage == -1) {
  targetPage = numPages;
  targetSlot = 0;
}
//}

@<code>{targetPage} と @<code>{targetSlot} は「これから書き込むページ番号とスロット番号」です。初期値の @<code>{-1} は「まだ書き込み先が決まっていない」という目印で、空きスロットを最初に見つけたときにその位置が入ります。ループを最後まで回しても @<code>{targetPage} が @<code>{-1} のままなら、既存のページがすべて埋まっているということなので、ファイルの末尾に新しいページ（ページ番号は @<code>{numPages}）を足し、その先頭スロット（0番）を使います。

書き込み先が決まったら、そのページを読み込み、スロットにレコードを書き込みます。1件を書き込む処理は「固定長スロットによるレコードの設計」で見たとおり、有効フラグ・キー・値の長さ・値の本体の順に @<code>{ByteBuffer} で並べるだけです。最後に、変更したページだけをファイルへ書き戻します。

//emlist[対象ページを読み込み、書き込み、書き戻す][java]{
// 対象のページを読み込む（新規ページなら読み込みは不要）
byte[] buf = new byte[PAGE_SIZE];
if (targetPage < numPages) {
  file.seek((long) targetPage * PAGE_SIZE);
  file.read(buf);
}

// スロットにレコードを書き込む
int offset = targetSlot * SLOT_SIZE;
ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
bb.put((byte) 1); // 有効フラグ
bb.putInt(id); // キー
byte[] valBytes = value.getBytes(StandardCharsets.UTF_8);
int len = Math.min(valBytes.length, 55);
bb.putInt(len); // 値の長さ
bb.put(valBytes, 0, len); // 値の本体

// 対象ページだけをファイルに上書き保存
file.seek((long) targetPage * PAGE_SIZE);
file.write(buf);
//}

新しいページを作る場合（@<code>{targetPage} が @<code>{numPages} と等しいとき）は、まだファイルに存在しないページなので読み込みをスキップし、空のバイト配列にそのまま書き込みます。既存ページの空きスロットに書く場合は、他のスロットのデータを消さないよう、いったんページ全体を読み込んでから対象スロットだけを書き換えます。

第2章では更新のたびに全レコードを書き出していましたが、ここで最後にファイルへ書き込んでいるのは4096バイト、つまり1ページ分だけです。データベース全体に何万件のレコードがあっても、1回の insert で書き込む量は常に1ページで一定になります。ファイル全体の書き直しという無駄は、これで解消できました。

=== 検索（select）

まず全件表示から見ていきます。処理は次の手順で進みます。

 1. 現在のファイルサイズから、ページ数を求める。
 2. ページを先頭から順に1枚ずつ読み込む。
 3. ページ内の各スロットの有効フラグを見て、1（使用中）のものだけを取り出す（0の空きスロットは飛ばす）。
 4. 取り出したスロットからキーと値を読み出し、表示する。

フラグが0のスロットは空きなので飛ばし、使用中のスロットだけを表示します。

//emlist[全件を表示する select][java]{
for (int p = 0; p < numPages; p++) {
  byte[] buf = new byte[PAGE_SIZE];
  file.seek((long) p * PAGE_SIZE);
  file.read(buf);

  for (int s = 0; s < MAX_SLOTS; s++) {
    int offset = s * SLOT_SIZE;
    if (buf[offset] == 1) { // 使用中スロットのみ読み取る
      ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
      bb.get(); // フラグスキップ

      int key = bb.getInt();
      int valLen = bb.getInt();
      byte[] valBytes = new byte[valLen];
      bb.get(valBytes);
      String value = new String(valBytes, StandardCharsets.UTF_8);
      System.out.println("(" + key + "," + value + ")");
    }
  }
}
//}

読み出しの順番は、書き込んだときとまったく同じ「フラグ → キー → 値の長さ → 値の本体」です。@<code>{bb.get()} でフラグを1バイト読み飛ばし、@<code>{bb.getInt()} でキー、もう一度 @<code>{bb.getInt()} で値の長さを取得します。ここで取り出した @<code>{valLen} が重要で、@<b>{値の本体は固定の55バイトではなく、この長さの分だけ読み出します}。書き込んだときの長さを覚えているからこそ、余分なバイトを含めずに元の値を正しく復元できるわけです。

キーを指定した検索も、ページとスロットを順に調べていく流れは同じです。処理の手順は次のとおりです。

 1. ページを先頭から順に1枚ずつ読み込む。
 2. 各使用中スロットのキーを取り出し、指定したキーと一致するか調べる。
 3. 一致したらそのレコードの値を表示し、処理を打ち切る。
 4. 最後まで一致しなければ「見つからなかった」と表示する。

全件表示との違いは、@<b>{目的のキーを持つスロットが見つかった時点で、そのレコードを表示して処理を打ち切る}点です。

//emlist[キーを指定した検索][java]{
for (int p = 0; p < numPages; p++) {
  byte[] buf = new byte[PAGE_SIZE];
  file.seek((long) p * PAGE_SIZE);
  file.read(buf);

  for (int s = 0; s < MAX_SLOTS; s++) {
    int offset = s * SLOT_SIZE;
    if (buf[offset] == 1) {
      ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
      bb.get(); // フラグスキップ
      int recordKey = bb.getInt();

      if (recordKey == id) {
        int valLen = bb.getInt();
        byte[] valBytes = new byte[valLen];
        bb.get(valBytes);
        String value = new String(valBytes, StandardCharsets.UTF_8);
        System.out.println(value);
        return;
      }
    }
  }
}
System.out.println("Record not found.");
//}

使用中スロットのキーが指定したキーと一致すれば、値を読み出して表示し、すぐに @<code>{return} で探索を打ち切ります。最後まで調べても見つからなければ @<code>{Record not found.} と表示します。ここで注目したいのは、@<b>{目的のレコードが後ろのページにあるほど、それまでのページをすべて読み込んでから見つかる}という点です。この性質が、本章の最後で述べる課題につながります。

=== 更新（update）

update は、既存のレコードの@<b>{値だけ}を新しい値に書き換えるコマンドです。キーそのものは変えません。処理の流れは次のとおりです。

 1. ページを順に読み込み、指定したキーを持つ使用中スロットを探す。
 2. 見つかったら、そのスロットの「値の長さ」と「値の本体」を新しい値で上書きする。
 3. そのページだけをファイルへ書き戻す。

//emlist[レコードの値を上書きする][java]{
if (recordKey == id) {
  // 対象を見つけたら同じスロットに上書き
  byte[] valBytes = value.getBytes(StandardCharsets.UTF_8);
  int len = Math.min(valBytes.length, 55);

  bb.putInt(len);
  bb.put(valBytes, 0, len);

  // 対象ページだけを保存
  file.seek((long) p * PAGE_SIZE);
  file.write(buf);

  System.out.println("Updated correctly");
  return;
}
//}

ここで少し細かい点を補足します。このコードに入る直前で、キーを確かめるために @<code>{bb.get()}（フラグ）と @<code>{bb.getInt()}（キー）をすでに実行しています。その結果、@<code>{ByteBuffer} の読み書き位置は@<b>{ちょうど「値の長さ」の手前}まで進んでいます。そのため、続けて @<code>{bb.putInt(len)} と @<code>{bb.put(valBytes, 0, len)} を呼ぶだけで、フラグとキーはそのまま残したまま、値の部分だけをきれいに上書きできます。レコードは同じスロットにとどまるので位置を動かす必要はなく、書き戻すのはやはりそのレコードを含む1ページだけです。

=== 削除（delete）

delete は、指定したキーのレコードを削除するコマンドです。ここでは、スロットの中身を実際に消したり、後ろのレコードを前に詰めたりはしません。その代わり、@<b>{スロット先頭の有効フラグを0にするだけ}で削除とみなします。これを@<b>{論理削除}と呼びます。

//emlist[有効フラグを0にする論理削除][java]{
if (recordKey == id) {
  // 対象を見つけたら、先頭フラグを0にして論理削除
  buf[offset] = 0;

  file.seek((long) p * PAGE_SIZE);
  file.write(buf);

  System.out.println("Deleted and saved to disk.");
  return;
}
//}

フラグを0にすると、そのスロットは以降「空き」として扱われます。select はフラグが1のスロットしか表示しないので論理削除したレコードは検索結果から消え、insert は空きスロットを再利用するのでこのスロットは次の挿入で上書きされます。キーや値のバイトはフラグを立て直すまでスロットに残りますが、有効フラグが0である限り無効なデータとして無視されるため、問題はありません。1バイトを書き換えるだけで削除が完了し、ここでも書き戻すのは1ページだけです。

=== 処理時間の計測

本章のREPLでは、コマンドを実行するたびにかかった時間を表示するようにしています。

//emlist[コマンドの実行時間を計測する][java]{
// --- 計測開始 ---
long startTime = System.nanoTime();

// （ここでコマンドを実行）

// --- 計測終了 ---
long endTime = System.nanoTime();
double timeMs = (endTime - startTime) / 1_000_000.0;
System.out.printf("(Executed in %.3f ms)\n", timeMs);
//}

この表示によって、それぞれの操作にどれくらいの時間がかかっているかを実際に目で確かめられます。次章に進む前に、レコードを増やしながら検索の時間がどう変化するかを観察しておくと、この後の課題が実感しやすくなります。

== まとめと次章への課題

本章では、データベースのデータ配置を次のように改善しました。

 * ファイルを4096バイトの@<b>{ページ}という固定長のブロックに区切り、目的のページへ直接アクセスできるようにした。
 * 1ページを64バイトの@<b>{固定長スロット}に分割し、s番目のスロットの位置を「s × 64」で即座に計算できるようにした。
 * 挿入・更新・削除で書き込むのは、変更のあった@<b>{1ページ分だけ}になり、ファイル全体を書き直す無駄がなくなった。

一方で検索には依然として課題が残っています。第3章までのHashMapによるデータ管理ではなくなったため、キーを指定してレコードを探すときファイルの先頭から順に目的のキーが見つかるまですべてのページとスロットを調べています。挿入時の重複チェックも同じように全体を走査しています。
このように、先頭から1件ずつ順番に調べていく探索を@<b>{線形探索（Linear Search）}と呼びます。線形探索でかかる時間はレコードの件数に比例します。データが数十件であれば一瞬ですが、数万件、数百万件と増えていくと、1件を探すだけのために毎回大量のページを読み込むことになり、実用的な速度ではなくなってしまいます。本章で追加した実行時間の表示を使えば、件数を増やすほど検索が遅くなっていく様子を確認できるはずです。

次章では、この問題を解決するために@<b>{B-Tree（B木）}を導入します。ファイルの先頭から順に探すのではなく、探しているキーがファイルのどこにあるのかを一足飛びに知る仕組みを組み込み、検索を高速化していきます。