= ページとスロットによるデータ配置の改善

== 第3章までの課題：ファイル全体の書き直し

第3章では、プログラムを終了してもデータが消えないように、レコードをファイルへ保存する仕組みを作りました。

一方で、insert, update, delete によりレコードを1件でも変更したらファイル全体を書き直しているという点が非効率です。10000件のレコードがある状態で1件を更新すると、変更のなかった9999件も含めてすべてを書き出すことになります。書き込む量はレコードの総数に比例して増えていくため、データが増えるほど1回の更新が重くなっていきます。

これは保存の単位がファイル全体になっているためです。データベースが扱うデータは、ファイルの先頭から末尾まで1本のテキストとして並んでいるだけなので、途中の1件だけを差し替える手段がありません。

== 本章の方針：ページ単位での読み書き

この課題を解決するには、保存の単位をファイル全体よりも小さくし、変更したい部分だけを読み書きできるようにする必要があります。

そこで本章では@<b>{ページ}を導入し、更新のたびにファイル全体を書き直すのではなく、@<b>{変更のあった部分だけを読み書きする}実装へと作り替えます。具体的には、次の順序で進めます。

 1. ファイルを@<b>{ページ}という固定長の単位に区切り、目的のページへ直接アクセスできるようにする。
 2. さらに、1ページの中をスロットという固定長の区画に分割し、レコードを決まった位置へ格納できるようにする。
 3. CRUDをページ単位の読み書きで実装し直す。

この作り替えにより、レコードを1件変更するときにファイルへ書き込む量は、データの総数に関わらず1ページ分で一定になります。次節ではまず、ページという単位の導入から見ていきます。

== ページの導入と固定長スロットによるレコードの設計

ページとはファイルを一定の大きさで区切った固定長の単位のことです。本書では1ページを4096バイト（4KB）とし、ファイルの先頭から先頭から4096バイトずつ「ページ0」「ページ1」「ページ2」……と区切って管理します。
これにより、あるページのファイルの先頭からのバイト位置は「ページ番号@<m>{\times 4096}」で即時に計算できるため、@<code>{RandomAccessFile} の @<code>{seek()} を使って目的のページへ直接移動し、そのページだけを読み書きできます。

さらに、@<img>{chapter04_image01}に示すように1ページ（4096バイト）の中を64バイトずつの区画に区切ります。この1区画を@<b>{スロット}と呼び、1つのスロットに1件のレコードを格納します。1ページは @<m>{4096 / 64 = 64} 個のスロットを持ちます。

//image[chapter04_image01][ページとスロットの構成][scale=0.75]{
//}

ページが固定長であることと同様にスロットも固定長であることで、スロットの先頭からのバイト位置は「s@<m>{\times 64}」で計算できます。さらに、様々な長さの値をその長さによらず一律に格納でき、値の更新があっても他のレコードの格納位置には影響を及ぼしません。
スロットの64バイトの内訳を@<img>{chapter04_image02}に示します。

//image[chapter04_image02][スロットの内訳][scale=0.75]{
//}

先頭の1バイトは@<b>{有効フラグ}です。そのスロットが使用中か空きかを表し、レコードを読み書きするときはまずこのフラグを確認します。続く4バイトにキー（整数）、次の4バイトに値のデータ長、残りの55バイトに値の本体を格納します。@<code>{1 + 4 + 4 + 55} でちょうど64バイトになります。

Javaでこのバイト列を組み立てるには @<code>{ByteBuffer} を使用します。ページを表すバイト配列の中から、「対象のスロット番号 @<m>{\times} スロットサイズ」を先頭として @<code>{ByteBuffer} を用意し、フラグ・キー・長さ・本体の順に書き込みます。

//emlist[スロットへ1件のレコードを書き込む][java]{
int offset = targetSlot * SLOT_SIZE;
ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
bb.put((byte) 1); // 有効フラグをセット
bb.putInt(id); // キーをセット

byte[] valBytes = value.getBytes(StandardCharsets.UTF_8);
int len = Math.min(valBytes.length, 55); // 最大55バイトに制限
bb.putInt(len);
bb.put(valBytes, 0, len);
//}
値に割り当てられた領域が55バイトであるため、@<code>{Math.min(valBytes.length, 55)} で最大55バイトに切り詰めています。読み出すときは、保存しておいた「値の長さ」の分だけバイトを取り出して文字列に戻します。

== ページ単位での読み書きの実装

スロットの設計が決まったので、CRUDをページ単位の読み書きで実装していきます。4つのコマンドはどれも、@<b>{ページを読み込む → スロットを調べる（または書き換える）→ 必要ならページを書き戻す}という共通の流れで動きます。書き込みが必要なときでも、ファイルへ書き戻すのは変更のあった1ページだけです。

以降、コマンドごとに処理の流れを順を追って見ていきます。

==== insert

 1. 現在のファイルサイズから、ページ数を求める。
 2. すべてのページ・スロットを調べ、同じキーがすでに存在しないか確認し、最初に見つかった空きスロットの位置を覚えておく。
 3. 空きスロットが1つも無ければ、新しいページを1枚増やしてその先頭スロットを使う。
 4. 書き込み先のページを読み込み、スロットにレコードを書き込む。
 5. そのページだけをファイルへ書き戻す。

1〜3が、書き込み先を決める部分です。ページを1枚ずつ読み込みながら、各スロットの先頭バイト（有効フラグ）を見て「使用中」か「空き」かを判断します。使用中スロットならキーを取り出して重複していないか確かめ、空きスロットなら（まだ候補を記録していなければ）その位置を書き込み候補として覚えます。

//emlist[重複チェックと空きスロットの探索][java]{
int numPages = (int) Math.ceil((double) file.length() / PAGE_SIZE);

int targetPage = -1;
int targetSlot = -1;

for (int p = 0; p < numPages; p++) {
  byte[] buf = new byte[PAGE_SIZE];
  file.seek((long) p * PAGE_SIZE);
  file.read(buf);

  for (int s = 0; s < MAX_SLOTS; s++) {
    int offset = s * SLOT_SIZE;
    if (buf[offset] == 1) { // 使用中スロット
      ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
      bb.get(); // 有効フラグをスキップ
      if (bb.getInt() == id) {
        System.out.println("Key already exists. Use update command to modify.");
        return;
      }
    } else if (buf[offset] == 0 && targetPage == -1) { // 最初の空きスロットを記録
      targetPage = p;
      targetSlot = s;
    }
  }
}

// 空きがない場合は新しいページを用意する
if (targetPage == -1) {
  targetPage = numPages;
  targetSlot = 0;
}
//}

@<code>{targetPage} と @<code>{targetSlot} は「これから書き込むページ番号とスロット番号」です。初期値の @<code>{-1} は「まだ書き込み先が決まっていない」という目印で、空きスロットを最初に見つけたときにその位置が入ります。ループを最後まで回しても @<code>{targetPage} が @<code>{-1} のままなら、既存のページがすべて埋まっているということなので、ファイルの末尾に新しいページ（ページ番号は @<code>{numPages}）を足し、その先頭スロット（0番）を使います。

書き込み先が決まったら、そのページを読み込み、スロットにレコードを書き込みます。1件を書き込む処理は「固定長スロットによるレコードの設計」で見たとおり、有効フラグ・キー・値の長さ・値の本体の順に @<code>{ByteBuffer} で並べます。最後に、変更したページだけをファイルへ書き戻します。

//emlist[対象ページを読み込み、書き込み、書き戻す][java]{
// 対象のページを読み込む（新規ページなら読み込みは不要）
byte[] buf = new byte[PAGE_SIZE];
if (targetPage < numPages) {
  file.seek((long) targetPage * PAGE_SIZE);
  file.read(buf);
}

// スロットにレコードを書き込む
int offset = targetSlot * SLOT_SIZE;
ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
bb.put((byte) 1); // 有効フラグ
bb.putInt(id); // キー
byte[] valBytes = value.getBytes(StandardCharsets.UTF_8);
int len = Math.min(valBytes.length, 55);
bb.putInt(len); // 値の長さ
bb.put(valBytes, 0, len); // 値の本体

// 対象ページだけをファイルに上書き保存
file.seek((long) targetPage * PAGE_SIZE);
file.write(buf);
//}

新しいページを作る場合（@<code>{targetPage} が @<code>{numPages} と等しいとき）は、まだファイルに存在しないページなので読み込みをスキップし、空のバイト配列にそのまま書き込みます。既存ページの空きスロットに書く場合は、他のスロットのデータを消さないよう、いったんページ全体を読み込んでから対象スロットだけを書き換えます。

第2章では更新のたびに全レコードを書き出していましたが、ここで最後にファイルへ書き込んでいるのは4096バイト、つまり1ページ分だけです。データベース全体に何万件のレコードがあっても、1回の insert で書き込む量は常に1ページで一定になります。前章での課題であった、レコードの更新のたびにファイル全体を書き直すという非効率性は、これで解消できました。

==== select

まず全件表示の流れから見ていきます。

 1. 現在のファイルサイズから、ページ数を求める。
 2. ページを先頭から順に1枚ずつ読み込む。
 3. ページ内の各スロットの有効フラグを見て、1（使用中）のものだけを取り出す（0の空きスロットは飛ばす）。
 4. 取り出したスロットからキーと値を読み出し、表示する。

フラグが0のスロットは空きなので飛ばし、使用中のスロットだけを表示します。

//emlist[全件を表示する select][java]{
for (int p = 0; p < numPages; p++) {
  byte[] buf = new byte[PAGE_SIZE];
  file.seek((long) p * PAGE_SIZE);
  file.read(buf);

  for (int s = 0; s < MAX_SLOTS; s++) {
    int offset = s * SLOT_SIZE;
    if (buf[offset] == 1) { // 使用中スロットのみ読み取る
      ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
      bb.get(); // フラグスキップ

      int key = bb.getInt();
      int valLen = bb.getInt();
      byte[] valBytes = new byte[valLen];
      bb.get(valBytes);
      String value = new String(valBytes, StandardCharsets.UTF_8);
      System.out.println("(" + key + "," + value + ")");
    }
  }
}
//}

読み出しの順番は、書き込んだときとまったく同じ「フラグ → キー → 値の長さ → 値の本体」です。@<code>{bb.get()} でフラグを1バイト読み飛ばし、@<code>{bb.getInt()} でキー、もう一度 @<code>{bb.getInt()} で値の長さを取得します。ここで取り出した @<code>{valLen} が重要で、@<b>{値の本体は固定の55バイトではなく、この長さの分だけ読み出します}。書き込んだときの長さを覚えているからこそ、余分なバイトを含めずに元の値を正しく復元できるわけです。

キーを指定した検索も、ページとスロットを順に調べていく流れは同じです。処理の手順は次のとおりです。

 1. ページを先頭から順に1枚ずつ読み込む。
 2. 各使用中スロットのキーを取り出し、指定したキーと一致するか調べる。
 3. 一致したらそのレコードの値を表示し、処理を打ち切る。
 4. 最後まで一致しなければその旨を表示する。

全件表示との違いは、@<b>{目的のキーを持つスロットが見つかった時点で、そのレコードを表示して処理を打ち切る}点です。

//emlist[キーを指定した検索][java]{
for (int p = 0; p < numPages; p++) {
  byte[] buf = new byte[PAGE_SIZE];
  file.seek((long) p * PAGE_SIZE);
  file.read(buf);

  for (int s = 0; s < MAX_SLOTS; s++) {
    int offset = s * SLOT_SIZE;
    if (buf[offset] == 1) {
      ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
      bb.get(); // フラグスキップ
      int recordKey = bb.getInt();

      if (recordKey == id) {
        int valLen = bb.getInt();
        byte[] valBytes = new byte[valLen];
        bb.get(valBytes);
        String value = new String(valBytes, StandardCharsets.UTF_8);
        System.out.println(value);
        return;
      }
    }
  }
}
System.out.println("Record not found.");
//}

使用中スロットのキーが指定したキーと一致すれば、値を読み出して表示し、すぐに @<code>{return} で探索を打ち切ります。最後まで調べても見つからなければ @<code>{Record not found.} と表示します。ここで注目したいのは、@<b>{目的のレコードが後ろのページにあるほど、見つけるまで時間がかかるという点です。この性質が、本章の最後で述べる課題につながります。

==== update

 1. ページを順に読み込み、指定したキーを持つ使用中スロットを探す。
 2. 見つかったら、そのスロットの「値の長さ」と「値の本体」を新しい値で上書きする。
 3. そのページだけをファイルへ書き戻す。

//emlist[レコードの値を上書きする][java]{
if (recordKey == id) {
  // 対象を見つけたら同じスロットに上書き
  byte[] valBytes = value.getBytes(StandardCharsets.UTF_8);
  int len = Math.min(valBytes.length, 55);

  bb.putInt(len);
  bb.put(valBytes, 0, len);

  // 対象ページだけを保存
  file.seek((long) p * PAGE_SIZE);
  file.write(buf);

  System.out.println("Updated correctly");
  return;
}
//}

ここで少し細かい点を補足します。このコードに入る直前で、キーを確かめるために @<code>{bb.get()}（フラグ）と @<code>{bb.getInt()}（キー）をすでに実行しています。その結果、@<code>{ByteBuffer} の読み書き位置は@<b>{ちょうど「値の長さ」の手前}まで進んでいます。そのため、続けて @<code>{bb.putInt(len)} と @<code>{bb.put(valBytes, 0, len)} を呼ぶだけで、フラグとキーはそのまま残したまま、値の部分だけをきれいに上書きできます。レコードは同じスロットにとどまるので位置を動かす必要はなく、書き戻すのはやはりそのレコードを含む1ページだけです。

==== delete

スロットの中身を実際に消したり、後ろのレコードを前に詰めたりはしません。その代わり、@<b>{スロット先頭の有効フラグを0にするだけ}で削除とみなします。これを@<b>{論理削除}と呼びます。

//emlist[有効フラグを0にする論理削除][java]{
if (recordKey == id) {
  // 対象を見つけたら、先頭フラグを0にして論理削除
  buf[offset] = 0;

  file.seek((long) p * PAGE_SIZE);
  file.write(buf);

  System.out.println("Deleted and saved to disk.");
  return;
}
//}

フラグを0にすると、そのスロットは以降「空き」として扱われます。select時はフラグが1のスロットのみ表示し、またinsert時にこのスロットを使用した場合は上書きされます。キーや値のバイトはフラグを立て直すまでスロットに残りますが、有効フラグが0である限り無効なデータとして無視されるため、問題はありません。1バイトを書き換えるだけで削除が完了し、ここでも書き戻すのは1ページだけです。

==== 処理時間の計測

insert, update, delete のたびにレコード全体をファイルに書き出していた前章と、ページとスロットを導入した本章の処理時間を比較します。

//table[performance_comparison][実行時間の比較（1万件のデータが存在する状態）]{
コマンド	前章	本章
-------------------------------------------------------------
@<code>{select 5000}	0.315 ms	9.468 ms
@<code>{insert 10001 user10001}	16.114 ms	4.622 ms
@<code>{update 5000 test}	8.025 ms	3.751 ms
@<code>{delete 5000}	9.833 ms	2.378 ms
//}

前章と比較し、insert, update, delete の処理時間が短くなっていることが確認できます。一方で、selectの処理時間は大幅に増加しています。これは、前章でのHashMapによる管理からページによる管理に変更したことで、どの操作においても先頭から1件ずつ順番に走査する処理となったためです。

== まとめと次章への課題

本章では、データベースのデータ配置を次のように改善しました。

 * ファイルを4096バイトの@<b>{ページ}という固定長の単位に区切り、ページの位置を「ページ番号@<m>{\times 4096}」で即時に計算可能となった。
 * 1ページを64バイトの@<b>{スロット}という固定長の単位に分割し、目的のスロットの位置を「スロット番号@<m>{\times 64}」で即座に計算可能となった。
 * 挿入・更新・削除で書き込むのは、変更のあった@<b>{1ページ分だけ}になり、データ更新のたびにファイル全体を書き直す無駄が解消された。

一方で検索には依然として課題が残っています。第3章までのHashMapによるデータ管理ではなくなったため、キーを指定してレコードを探すときファイルの先頭から順に目的のキーが見つかるまですべてのページとスロットを調べています。挿入時の重複チェックも同じように全体を走査しています。
このように、先頭から1件ずつ順番に調べていく探索を@<b>{線形探索（Linear Search）}と呼びます。線形探索でかかる時間はレコードの件数に比例します。データが数十件であれば一瞬ですが、数万件、数百万件と増えていくと、1件を探すだけのために毎回大量のページを読み込むことになり、実用的な速度ではなくなってしまいます。本章で追加した実行時間の表示を使えば、件数を増やすほど検索が遅くなっていく様子を確認できるはずです。

次章では、この問題を解決するために@<b>{B-Tree（B木）}を導入します。ファイルの先頭から順に探すのではなく、探しているキーがファイルのどこにあるのかを一足飛びに知る仕組みを組み込み、検索を高速化していきます。