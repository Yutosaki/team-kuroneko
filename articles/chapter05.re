= B-Treeによる検索の高速化と領域管理

== 第4章までの課題：線形探索の限界

第4章では、データを「ページ」という固定長のブロック単位で管理するようにデータベースを拡張しました。これにより、ディスクとの細かな入出力（I/O）が減り、効率的にデータを読み書きできるようになりました。

しかし、現在の実装には検索パフォーマンスにおいて大きな課題が残っています。それは、特定のレコードを探す（@<code>{select}）際や、更新・削除対象を見つける際に、@<b>{ファイルの先頭から最後まで、すべてのページとスロットを順番に調べている}という点です。

このような検索方法を@<b>{線形探索（Linear Search）}と呼びます。線形探索の処理時間はデータの件数 N に比例する O(N) となります。データが数十件程度であれば処理時間はごくわずかですが、数万件、数百万件と増えていくと、たった1件のデータを検索・更新するだけでも毎回膨大なディスク読み込み（フルスキャン）が発生し、実用的な速度ではなくなってしまいます。「なぜ遅いのか」の理由は、この「毎回すべてのデータに目を通しているから」という点にあります。

== 本章の方針

この「毎回すべてのデータを調べる」という線形探索の課題を解決するため、本章では検索に特化したデータ構造（インデックス）を導入し、目的のデータが「ディスク上のどこにあるか」を一瞬で特定できるようにします。

具体的には、以下の順序で実装を進めます。

 1. @<b>{B-Tree（B木）の導入と設計}: 検索キーと物理的な位置（RecordId）を紐づけるデータ構造として、ページ管理と相性の良い「B-Tree」を採用し、ノードの構造を定義します。
 2. @<b>{B-Treeアルゴリズムの実装}: 木のバランスを常に保つため、検索処理に加え、ノードの分割（Split）や統合（Merge）を伴う挿入・削除アルゴリズムを実装します。
 3. @<b>{起動時のインデックス構築と空き領域管理}: データベース起動時にファイルからB-Treeを構築し、同時に空きスロットを @<code>{freeList} として管理して、挿入時のI/Oコストも削減します。
 4. @<b>{CRUD操作の直接アクセス化}: これまでループで全件走査していたCRUD処理を、B-Treeを用いたピンポイントな直接アクセスへと書き換え、どれくらい高速化したかを計測します。

このステップを踏むことで、データ量が1万件、100万件と増えても、常に安定した速度で読み書きができる本格的なストレージエンジンが完成します。次節からはまず、「なぜ専用のデータ構造が必要になるのか」という基本的な考え方から見ていきましょう。

== 検索を高速化するデータ構造の導入

特定のデータへ直接アクセス（ランダムアクセス）するためには、データ本体のファイルとは別に、「探しているデータがファイルの@<b>{どこ}にあるのか」をあらかじめ整理したデータ構造（対応表）を導入する必要があります。

100万件のデータから特定のIDを探すために、巨大なデータ本体を先頭から読み込むのは非常に非効率です。キーと場所のペアだけを抽出したコンパクトな対応表を用意しておけば、本体を探索するコストを劇的に下げることができます。現実世界で例えるなら、分厚い専門書を1ページ目から順番に読むのではなく、巻末の「索引」を見てページ番号を調べ、直接そのページを開くのと同じアプローチです。

プログラム上では、以下のような@<b>{「検索キー」と「データの物理的な場所」のペア}を管理する対応表を作成してこれを実現します。

//cmd{
[ 検索キー ]    | [ データの物理的な場所 ]
ユーザーID: 10   | ページ2、スロット4
ユーザーID: 15   | ページ8、スロット1
ユーザーID: 8765 | ページ120、スロット3
...
//}

ユーザーが「IDが8765のデータを検索したい」と要求した場合、データベースは以下のように動きます。

 1. 巨大なデータ本体を読む前に、まずはこのコンパクトな対応表を探索し、「ID: 8765」を見つけ出します。
 2. そこに書かれている物理的な場所（ページ120、スロット3）を取得します。
 3. データ本体のファイルから、@<b>{ページ120だけをピンポイントでディスクから読み込み}、該当スロットのデータを取得します。

次節からは、この仕組みをJavaのプログラムとして具体的にどのように実装していくのかを見ていきます。

== B-Treeの採用：RecordIdの導入

プログラム上でこの仕組みを実現するために、まずは物理的な場所を @<code>{RecordId} クラスとして表現します。そして、検索キーと @<code>{RecordId} のペアを管理するデータ構造として、配列や二分探索木ではなく@<b>{B-Tree（B木）}を採用します。
（※実際の商用データベースでは範囲検索に最適化された派生型「B+Tree」が主流ですが、本章ではその基礎となるB-Treeを実装します。また、正式な「インデックス」という概念については第6章以降で扱います。）

データ構造として配列やリストを使用すると、途中にデータを追加・削除するたびに全体をシフトさせる必要があり非効率です。また「二分探索木」は、1つのノードに1つのキーしか持たないため、データが増えると木が「縦に深く」成長してしまい、ディスクに保存した場合にページ読み込み（I/O）回数が膨大になります。

対してB-Treeには、第4章で学んだ@<b>{「ページ（ブロック）」との相性が良い}という決定的な強みがあります。具体的には以下の理由が挙げられます。

 * @<b>{1つのノードに複数のキーを持てる（マルチウェイ）:} B-Treeは1つのノードに数十〜数百個のキーを配列として格納します。これを1ページのサイズ（4KB等）に合わせることで、1回のディスク読み込みで大量のキーを一気にメモリへ読み込めます。
 * @<b>{階層が極めて浅い（横に広い）:} 1ノードに100個のキー（101本の枝）を持てば、高さ3階層で100万件のデータを表現できます。わずか最大3回のディスクI/Oで目的のデータに到達できます。
 * @<b>{常にバランスを保つ（平衡木）:} データの増減に合わせて自動で木を再構成するため、常に一定の速度（O(log N)）が保証されます。

実装としては、ファイル上の「どこ」を示す位置情報を保持するために、ページ番号とスロット番号を持つ @<code>{RecordId} クラスを新しく定義します。

//emlist[位置情報を表すRecordId][java]{
/** レコードの物理的な位置情報を保持する識別子 (Record ID) */
static class RecordId {
  int page;
  int slot;

  RecordId(int page, int slot) {
    this.page = page;
    this.slot = slot;
  }
}
//}

また、B-Treeの全体構造とノードの中身を可視化すると以下の図のようになります。（※見やすさのため、1ノードあたりのキー数を2個までに絞っています）

//emlist[B-Treeの全体構造（高さ3の例）]{
                     [ 50 ]
                   /         \
                 /             \
       [ 20 | 35 ]             [ 65 | 80 ]
        /   |   \                 /   |   \
      /     |     \             /     |     \
[10|15] [25|30] [40|45]  [55|60] [70|75] [85|90]
//}

一番上にあるのが「ルートノード（根）」、中間が「内部ノード」、一番下が「リーフノード（葉）」です。各ノード内のキーが標識の役割を果たし、大小比較を行って進むべき枝を絞り込みながら末端へと到達するのが、B-Treeによる検索メカニズムです。

== B-Treeの実装

B-Treeをデータベースのデータ構造として機能させるには、検索だけでなく、更新操作に合わせて木構造のバランスを維持する仕組みが必要です。以降では、ノードの構造から順に、「検索」「挿入と分割」「削除と補充・統合」の各操作がどのような意図で実装されているのかを解説します。

==== ノードの表現（BTreeNode）と最小次数

ノード（@<code>{BTreeNode}）の実装にあたっては、キーとポインタの配列を持たせます。さらに、「最小次数（@<code>{t}）」と呼ばれるパラメータを導入し、1ノードが持てる最大キー数を @<code>{2*t - 1}、最小キー数を @<code>{t - 1} と定義します。

なぜ最大キー数を @<code>{2*t - 1} とするという中途半端な数式にするのでしょうか。それは、ノードが満杯になって「2つに分割（Split）」される際の要件を美しく満たすためです。
満杯のノード（@<code>{2*t - 1}個のキー）を左右に分割し、真ん中の1つのキーを親に渡すと、残された左右のノードのキー数はどちらもピッタリ「@<code>{t - 1}」になります。分割直後でもノードが無駄なく最小基準を満たし、木全体のバランス（50%以上の使用率）を維持できるように計算されているのです。

実際のデータを格納する「箱」であるノードは次のように定義されます。

//emlist[BTreeNodeクラスの実装][java]{
/** B-tree ノード */
static class BTreeNode {
  int[] keys;           // キー配列 (ユーザーID)
  RecordId[] values;    // レコードポインタ配列 (保存場所)
  BTreeNode[] children; // 子ノードへのポインタ配列
  int numKeys;          // 現在のノードに入っているキーの数
  boolean isLeaf;       // 葉ノード（最下層）かどうか

  BTreeNode(int t, boolean isLeaf) {
    this.isLeaf = isLeaf;
    this.keys = new int[2 * t - 1];          // 最大キー数は 2t - 1
    this.values = new RecordId[2 * t - 1];
    this.children = new BTreeNode[2 * t];    // 最大子ポインタ数は 2t
    this.numKeys = 0;
  }
}
//}

ノード内のキーが @<code>{k} 個あるとき、区切る枝の数は必ず @<code>{k + 1} 本になるため、ポインタ配列 @<code>{children} の最大サイズはキーの最大数に1を足した @<code>{2 * t} と定義されています。

==== 検索処理（Search）

目的のレコードを探す際は、ルートノードから開始し、各ノード内のキー配列を先頭から比較して探しているキーの範囲を見つけ出し、該当する子ノードへと再帰的に下っていくアプローチをとります。

ノード内での配列探索（線形探索）はサイズが小さくCPUキャッシュ上で一瞬で終わるのに対し、ディスクから巨大なページを読むコストは膨大です。そのため、ディスクI/Oの回数を「木の高さ（深さ）」の数回に抑えることが、データベースにおいて最も効率的な検索方法となります。

//emlist[BTreeクラスの検索処理][java]{
public RecordId search(int key) {
  return root == null ? null : search(root, key);
}

private RecordId search(BTreeNode node, int key) {
  int i = 0;
  // ノード内のキーを順番に比較し、進むべき枝（インデックス i）を探す
  while (i < node.numKeys && key > node.keys[i]) i++;

  // キーが一致すれば、その位置のRecordIdを返す
  if (i < node.numKeys && key == node.keys[i]) {
    return node.values[i];
  }

  // 葉ノードまで到達して見つからなければ、存在しない
  if (node.isLeaf) return null;

  // 対象の子ノードへ進み、再帰的に検索を続ける
  return search(node.children[i], key);
}
//}

==== 挿入とノードの分割（Split）

データを挿入する際、対象のノードが最大キー数に達して満杯だった場合、そのままでは新しいデータを追加できません。そこで、ノードを2つに分割（Split）し、真ん中のキーを親ノードに押し上げます。

これは、ノードの容量上限を超えないようにしつつ、「下から上へ」と木を成長させるためです。上へ持ち上がるように成長することで、すべての葉ノードまでの深さが常に均一に保たれ、バランスの崩れを防ぐことができます。

最小次数 @<code>{t=2}（最大キー数3）の満杯ノード @<code>{[10 | 20 | 30]} に分割が発生する様子を示します。

//cmd{
[ 分割前 ]
親ノード:        [ 50 ]
              /
満杯のノード: [ 10 | 20 | 30 ]  <-- ここに分割処理(splitChild)を実行

[ 分割後 ]
真ん中の「20」が親へ押し上げられ、残りが左右の子になる
親ノード:        [ 20 | 50 ]
              /      \
            /          \
新ノード左: [ 10 ]      新ノード右: [ 30 ]
//}

実際のプログラムでは、「配列要素の右シフト（隙間づくり）」と「データの移動」によってこの分割処理を実現します。

//emlist[splitChild メソッドの実装（一部省略）][java]{
void splitChild(BTreeNode parent, int i, BTreeNode fullNode) {
  // 1. 右半分を格納する新しいノードを作成
  BTreeNode newNode = new BTreeNode(t, fullNode.isLeaf);
  newNode.numKeys = t - 1;

  // 2. 満杯ノードの「右半分」のキーと値を、新ノードにコピー
  for (int j = 0; j < t - 1; j++) {
    newNode.keys[j] = fullNode.keys[j + t];
    newNode.values[j] = fullNode.values[j + t];
  }
  // ※内部ノードの場合は子ポインタ(children)のコピー処理も必要です

  fullNode.numKeys = t - 1; // 満杯ノードは「左半分」だけになる

  // 3. 親ノードのポインタ配列を右にズラし、新ノードを挿入する隙間を作る
  for (int j = parent.numKeys; j >= i + 1; j--) {
    parent.children[j + 1] = parent.children[j];
  }
  parent.children[i + 1] = newNode;

  // 4. 親ノードのキー配列も右にズラし、満杯ノードの「真ん中のキー」を押し上げる
  for (int j = parent.numKeys - 1; j >= i; j--) {
    parent.keys[j + 1] = parent.keys[j];
    parent.values[j + 1] = parent.values[j];
  }
  parent.keys[i] = fullNode.keys[t - 1]; // 真ん中のキーを親へ
  parent.values[i] = fullNode.values[t - 1];

  parent.numKeys++; // 親のキー数が1つ増える
}
//}

==== 削除処理におけるバランスの維持（Borrow と Merge）

データを削除した結果、キーの数が最小基準（@<code>{t - 1}）を下回った場合（アンダーフロー）、隣接ノードからキーを移動させる「補充（Borrow）」、または隣接ノードと1つにまとめる「統合（Merge）」を行います。

これはB-Treeの厳格な最小キー数のルールを守るためです。この調整を行わないと、キーの少ないスカスカなノードが大量に発生してしまい、結果的に木が深くなって検索パフォーマンスが悪化してしまいます。

補充（Borrow）では、大小関係を保つために直接横に移動させるのではなく、「親のキーを自分に降ろし、隣接ノードのキーを親に引き上げる」という回転動作を行います。

//cmd{
[ 補充前 ] 
親ノード:          [ 40 ]
                 /      \
左の隣接ノード(余裕あり):  対象ノード(不足):
[ 10 | 20 | 30 ]        [ 50 ]

[ 補充後 ] 
親の「40」が対象ノードに降り、左隣接ノードの最大値「30」が親に上がる
親ノード:          [ 30 ]
                 /      \
左の隣接ノード:            対象ノード:
[ 10 | 20 ]             [ 40 | 50 ]
//}

//emlist[borrowFromPrev メソッドの実装][java]{
void borrowFromPrev(BTreeNode node, int idx) {
  BTreeNode child = node.children[idx];
  BTreeNode sibling = node.children[idx - 1]; // 左の隣接ノード

  // 1. childのキー配列を右に1つズラして空きを作る
  for (int i = child.numKeys - 1; i >= 0; --i) child.keys[i + 1] = child.keys[i];

  // 2. 親ノードのキーを、childの先頭に降ろす
  child.keys[0] = node.keys[idx - 1];

  // 3. 左の隣接ノード(sibling)の最後のキーを、親ノードに引き上げる
  node.keys[idx - 1] = sibling.keys[sibling.numKeys - 1];

  child.numKeys++;
  sibling.numKeys--;
}
//}

一方、隣接ノードに余裕がない場合は、親の境界キーを降ろし、対象ノードと隣接ノードを1つにまとめる統合（Merge）を行います。

//emlist[merge メソッドの実装][java]{
void merge(BTreeNode node, int idx) {
  BTreeNode child = node.children[idx];
  BTreeNode sibling = node.children[idx + 1]; // 右の隣接ノード

  // 1. 親のキーを、childの末尾に降ろす（これが両者をつなぐ境界線）
  child.keys[t - 1] = node.keys[idx];

  // 2. 右の隣接ノードのすべてのキーを、childにコピー
  for (int i = 0; i < sibling.numKeys; ++i) child.keys[i + t] = sibling.keys[i];
  child.numKeys += sibling.numKeys + 1;

  // 3. 親ノードの配列を左に詰めて隙間を塞ぐ
  for (int i = idx + 1; i < node.numKeys; ++i) node.keys[i - 1] = node.keys[i];
  for (int i = idx + 2; i <= node.numKeys; ++i) node.children[i - 1] = node.children[i];
  node.numKeys--;
}
//}

== 起動時のB-Tree構築と空き領域管理

現在のプログラムではB-Treeがメモリ上にのみ存在するため、データベース終了時に揮発してしまいます。そのため、データベース起動時にファイル全体を1度だけ走査し、メモリ上にB-Treeを再構築する必要があります。また、その走査の過程でデータが存在しなかったスロットを @<code>{freeList} というキューに登録し、空き領域として管理します。

これまでの挿入（@<code>{insert}）処理では「空きスロットを探すための線形探索」がボトルネックになっていましたが、起動時に @<code>{freeList} を構築しておくことで、挿入時のI/Oコストを削減し高速化を図る狙いがあります。

起動時に実行される @<code>{buildTree} メソッドで、B-Treeの再構築と @<code>{freeList} の構築を同時に行います。

//emlist[起動時のB-Tree構築とfreeList登録][java]{
private Queue<RecordId> freeList;

private void buildTree() throws IOException {
  int numPages = (int) Math.ceil((double) file.length() / PAGE_SIZE);
  int count = 0;

  for (int p = 0; p < numPages; p++) {
    byte[] buf = new byte[PAGE_SIZE];
    file.seek((long) p * PAGE_SIZE);
    file.read(buf);

    for (int s = 0; s < MAX_SLOTS; s++) {
      int offset = s * SLOT_SIZE;
      if (buf[offset] == 1) {
        // データが存在する場合はB-Treeに登録
        ByteBuffer bb = ByteBuffer.wrap(buf, offset, SLOT_SIZE);
        bb.get(); // フラグをスキップ
        int recordKey = bb.getInt();
        btree.insert(recordKey, new RecordId(p, s));
        count++;
      } else {
        // フラグが0（空きスロット）の場合はfreeListに追加
        freeList.offer(new RecordId(p, s));
      }
    }
  }
  System.out.println("B-Tree build complete. Loaded " + count + " records.");
}
//}

== B-Treeを用いたCRUDの高速化

構築したB-Treeと @<code>{freeList} を活用し、実際のデータベース操作（@<code>{select}, @<code>{insert}, @<code>{update}, @<code>{delete}）の処理ロジックを、ループによる全件走査から直接アクセスへと書き換えます。

これにより、前章までの課題であった「線形探索によるパフォーマンス限界」を解消し、データ件数に依存しない O(log N) の安定した応答速度を実現します。

具体的には、検索・更新処理ではページ走査の @<code>{for} ループを削除し、@<code>{btree.search(id)} で取得した位置情報へ @<code>{file.seek()} で直接アクセスします。挿入処理では @<code>{freeList.poll()} から空き領域を即座に取得し、削除処理では空いた領域を @<code>{freeList.offer()} で再利用リストへ戻します。

//emlist[B-Treeを利用したselect処理の改善][java]{
public void select(int id) {
  try {
    // 1. B-Treeから位置情報 (RecordId) を取得
    RecordId rid = btree.search(id);
    if (rid == null) {
      System.out.println("Record not found.");
      return;
    }

    // 2. 対象のページのみをディスクから読み込む
    byte[] buf = new byte[PAGE_SIZE];
    file.seek((long) rid.page * PAGE_SIZE);
    file.read(buf);

    // 3. 対象スロットのデータを取得 (中略)
  } catch (IOException e) {
    System.out.println("Disk I/O Error during select.");
  }
}
//}

//emlist[B-TreeとfreeListを利用したinsert処理（一部抜粋）][java]{
// B-treeによる高速な重複チェック O(log N)
if (btree.search(id) != null) {
  System.out.println("Key already exists.");
  return;
}

int targetPage = -1;
int targetSlot = -1;

// freeリストから空きスロットを即座に取得 O(1)
if (!freeList.isEmpty()) {
  RecordId freeId = freeList.poll();
  targetPage = freeId.page;
  targetSlot = freeId.slot;
} else {
  // freeリストが空の場合は新規ページを作成 (中略)
}
//}

==== 実行速度の比較：どれくらい速くなったのか

ここまで実装してきた「B-Treeによる検索の高速化」と「@<code>{freeList}による空き領域管理」によって、実際のデータベース操作がどれほど劇的に改善されたのかを確認してみましょう。

あらかじめデータベースに1万件のデータ（ID: 1〜10000）を挿入した状態で、前章（線形探索）と本章（B-Tree＋freeList）のプログラムに対して同じコマンドを実行し、処理時間を比較しました。

//table[performance_comparison][実行時間の比較（1万件のデータが存在する状態）]{
コマンド	前章（線形探索）	本章（B-Tree + freeList）
-------------------------------------------------------------
select 8765	11.403 ms	0.686 ms
update 8765 test	5.933 ms	2.960 ms
delete 1	1.628 ms	0.991 ms
delete 8765	3.257 ms	0.983 ms
insert 10001 test	3.310 ms	1.218 ms
//}

結果は一目瞭然です。ファイルの先頭から順にページを読み込んでいた @<code>{select} 処理は、B-Treeを導入したことで @<b>{16倍以上もの高速化} を果たしています。

特に注目していただきたいのが @<code>{delete} の結果です。
前章の線形探索では、ファイルの先頭付近にある @<code>{delete 1}（1.628 ms）に比べて、末尾付近にある @<code>{delete 8765}（3.257 ms）は、対象を見つけるまでに多くのデータを読み込む必要があり、処理時間が2倍近くかかっていました。
しかし、本章の実装では @<code>{delete 1} も @<code>{delete 8765} も @<b>{どちらも約0.9 ms} で完了しています。B-Treeは木がバランスを保つ（すべての葉までの深さが同じになる）ため、@<b>{データがファイルのどこにあっても、常に一定の速度（O(log N)）でアクセスできる}という強力な特性が数値として美しく表れています。

また、新しいデータを追加する @<code>{insert} についても、挿入前の「キーの重複チェック」がB-Treeで高速化され、さらに書き込み先の確保も「空きスロットを探す線形探索」から「@<code>{freeList} から位置情報を取り出すだけ（O(1)）」に変わったことで、処理時間が大幅に短縮されています。

== まとめと次章への課題

本章では、データベースの検索・更新処理を高速化する「B-Tree」と、空き領域を効率的に再利用するための「@<code>{freeList}」を実装しました。

これらを導入したことにより、前章で直面した「線形探索によるパフォーマンスの限界」を見事に克服しました。検索対象のデータを特定する処理が O(log N) の時間計算量へと改善され、データ挿入時の空き領域探索にかかるディスクI/Oも削減されました。これにより、内部的なデータアクセスの仕組みは実践的な速度で動作するようになりました。

しかし、データベースの「内部の処理速度」は向上したものの、ユーザーがデータベースを操作するための「インターフェース」には課題が残されています。

 * @<b>{操作方法が複雑で扱いにくい:}
   現在の仕様では、データの挿入や検索を行うために、専用のメソッドや独自の低レイヤーなコマンドを直接呼び出す必要があります。これではアプリケーションから汎用的かつ直感的に利用することができません。

 * @<b>{「何を」取得するかは分かるが、「どう」実行するかが決まっていない:}
   現在は「B-Treeの @<code>{search} を呼んでからファイルを読む」という単純な手順が決め打ちになっています。今後「特定の条件を満たすレコードだけを取得したい」といった要求が出た場合、ユーザー自身が「どの仕組みを使って、どの順番でデータを読み込むか」という具体的な手続き（手段）まで意識しなければならなくなってしまいます。

現代のリレーショナルデータベースが広く使われている最大の理由は、ユーザーが「欲しいデータ（目的）」だけを宣言的に記述すれば、データベース側が自動的に「最適な検索手順（手段）」を判断して実行してくれる点にあります。

次章（第6章）では、この課題を解決するために @<b>{SQL} の処理エンジンをデータベースに組み込みます。「何をするか」を記述する標準言語であるSQLを読み解くため、字句解析（トークナイズ）や構文解析（パース）、そして抽象構文木（AST）の構築といった、言語処理系の領域へと足を踏み入れます。

まずは第一歩として、@<code>{SELECT} 文や @<code>{INSERT} 文をプログラムが理解できる形に変換する仕組みを作っていきましょう。
