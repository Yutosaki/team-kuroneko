= 真のデータベースに向けて

これまでの章を通して、私たちは何もない状態から一歩ずつ、データベースシステムの核となる仕組みを自作してきました。対話型の小さなプログラムから始まり、ディスクへの永続化、ページベースのストレージ管理、B-Treeによる高速な検索、SQLの構文解析からクエリプランナの実装、そして第8章でのアーキテクチャの整理まで、データベースの裏側で動いている要素技術を体験しました。

本章では、これまでの歩みを振り返るとともに、私たちが作成したデータベースを実運用に耐えうる本格的なシステムへと進化させるために「今足りないものは何か」、そして「それを今後どう実装していくのか」という方針について解説します。

== これまでの振り返り

私たちが構築したデータベースは、現在以下のような構成を持っています。

 * @<b>{ストレージエンジン（第3章〜第5章）}: データをファイルに永続化し、固定長スロットとページという単位でディスクを管理。さらにB-Treeを用いて、線形探索に頼らない高速なインデックスとデータ配置を実現しました。
 * @<b>{実行エンジン（第6章〜第7章）}: パーサ（構文解析）、プランナ（実行計画の選択）、クエリエグゼキュータ（クエリの実行）を実装し、ユーザーが入力したSQL文字列から最適な手順を導き出し、結果を返すパイプラインを構築しました。
 * @<b>{構造の分離と整理（第8章）}: システムが肥大化する中で、ストレージ管理、カタログ（メタデータ）管理、クエリ実行などの各コンポーネントの責任境界を明確にし、拡張性の高いアーキテクチャへとリファクタリング（オペレータの導入）を行いました。

これにより、単一のアプリケーションからローカルファイルとして直接扱う「組み込み型データベース（SQLiteのような形式）」の基礎は十分に完成しました。しかし、PostgreSQLやMySQLのような堅牢なデータベースサーバになるためには、まだいくつかの大きな壁があります。

== 今に足りないものと実装方針

私たちが作ったデータベースをさらに拡張するための、4つの大きなテーマとその実装方針を紹介します。

==== クライアント／サーバモデル（ネットワーク対応）

@<b>{【現状と課題】}
現在のシステムは、コマンドライン（REPL）上で直接プロセスとして動作しており、他のアプリケーションからネットワーク越しに利用することができません。

@<b>{【実装方針】}
データベースを常駐する「サーバプロセス」として独立させます。

 * @<b>{通信プロトコル}: TCP/IPソケット通信を用いて、複数クライアントからの接続を同時に受け付けるデーモンプログラムを作成します。
 * @<b>{ワイヤープロトコル}: クライアントからSQL文字列を受け取り、実行結果（カラム定義とRowの集合、またはエラー情報）をバイナリ形式でシリアライズして送り返す独自の通信ルール（プロトコル）を設計します。

==== トランザクションと同時実行制御（Concurrency Control）

@<b>{【現状と課題】}
複数のユーザーが同時にデータを読み書きすることを想定していません。同時に更新処理が走ると、データファイルが競合して破損する（不整合が起きる）危険性があります。

@<b>{【実装方針】}
ACID特性（特に分離性：Isolation）を満たすための制御機構を組み込みます。

 * @<b>{ロック機構の導入}: レコード単位やページ単位で、「共有ロック（Read Lock）」と「排他ロック（Write Lock）」を取得・解放するLock Managerを実装し、競合を防ぎます。
 * @<b>{MVCC（多版同時実行制御）}: さらに高度な方針として、データの更新時に上書きせず「新しいバージョンのレコード」を作成することで、読み取り処理と書き込み処理が互いをブロックしない仕組み（PostgreSQLなどが採用）の実装に挑戦するのも良いでしょう。

==== 障害復旧とログ（Crash Recovery）

@<b>{【現状と課題】}
メモリ上（バッファプール）にある変更がディスクに書き込まれる前にシステムがクラッシュしたり電源が落ちたりすると、データが失われたり、B-Treeの構造が途中で壊れたりします。

@<b>{【実装方針】}
トランザクションの永続性（Durability）と一貫性（Consistency）を保証するために、ログ先行書き込み（WAL: Write-Ahead Logging）を実装します。

 * @<b>{WALファイルの実装}: データファイル（ページ）を更新する前に、必ずシーケンシャルな「ログファイル」に変更内容を追記（Append）します。
 * @<b>{リカバリ機構}: データベースの起動時にWALを読み込み、クラッシュ前に完了していた処理を再実行（Redo）し、途中で終わっていた処理を取り消す（Undo）機構を実装します。

==== より高度なクエリオプティマイザとSQLサポート

@<b>{【現状と課題】}
現在のプランナは、インデックスの有無などの単純なルールベースで動いています。また、集約関数（COUNTなど）や複雑な条件には対応していません。

@<b>{【実装方針】}

 * @<b>{集約とソート}: @<code>{GROUP BY} や @<code>{ORDER BY} を処理するために、メモリ上でのハッシュ集約や、メモリに乗り切らない場合の外部ソートアルゴリズムを実行エンジンに追加します。
 * @<b>{コストベース・オプティマイザ（CBO）}: カタログに統計情報（テーブルの行数や、カラムの値の分布など）を保持させます。その情報をもとに、「フルスキャンとインデックススキャンのどちらがI/Oコストが低いか」を数学的に計算して最適な実行計画を選択するようにプランナを拡張します。

== おわりに：ブラックボックスを開けよう

本書の目的は、単に動くツールを作ることではなく、@<b>{「データベースという巨大なブラックボックスの蓋を開け、自分の手で仕組みを理解すること」}でした。

普段何気なく使っている @<code>{SELECT * FROM users WHERE id = 1;} というたった一行の裏側で、文字列がトークンに分解され、構文木が構築され、プランナが最適な道を選び、B-Treeのルートノードからリーフへと辿り、ページからスロットを読み取って結果を返すという、壮大なバケツリレーが行われています。

一度でも自らの手でデータベースを作り上げ、内部の構造を分離し整理したあなたは、今後PostgreSQLやMySQLを使う際、システムを単なる「ブラックボックス」ではなく、「アルゴリズムとデータ構造の結晶」として捉えられるようになっているはずです。

データベースの世界は深く、そして広大です。本書で築いた「最小のデータベース」を足がかりに、ぜひご自身の手で足りない機能を実装し、さらに高度なデータ基盤の世界へと足を踏み入れてみてください。
