= まえがき

現代のソフトウェア開発において、データベースはなくてはならない存在です。Webアプリケーション、スマートフォンアプリ、企業の基幹システムに至るまで、ありとあらゆるシステムがデータベースを利用しています。

しかし、アプリケーション開発者にとって、その内部はしばしば「ブラックボックス」になりがちです。SQLという呪文（クエリ）を投げると、データベースは裏側で良きに計らい、魔法のように高速にデータを返してくれます。「なぜインデックスを張ると速くなるのか」「内部でデータはどのような形でディスクに保存されているのか」を深く理解する機会は多くありません。

== 既存の学習手法の限界と本書のアプローチ

データベースの仕組みを学ぼうとして、既存の技術書を手に取ったことがあるかもしれません。しかし、従来のデータベース学習には以下のような壁がありました。

 * @<b>{理論が先行して難しすぎる}: B-Treeやリレーショナル代数などの学術的な理論ばかりが続き、コードを書く前に挫折してしまう。
 * @<b>{完成形しか見えない}: 「データベースとはこういうものだ」と最初から複雑なコードを見せられ、「なぜその設計が必要になったのか」という背景が分からない。
 * @<b>{コードが巨大すぎる}: PostgreSQLやMySQLなどの実際のソースコードを読もうとしても、数百万行の海に溺れてしまう。

そこで本書では、既存のアプローチとは全く異なる手法をとります。
私たちが目指すのは、最初から完璧なデータベースを作ることではありません。@<b>{「進化の過程（変化）」を楽しむこと}。これこそが、本書の最大のテーマです。

@<b>{「課題駆動型（Problem-Driven）」で、最小のコードから少しずつデータベースを育てていく}という体験を通して、技術がなぜ必要なのかを肌で感じてください。「データが増えると遅い」「更新のたびにファイル全体を書き直さなければならない」といった課題にぶつかり、先人たちが生み出したデータ構造やアルゴリズムを実装し、劇的にパフォーマンスが向上する快感を味わう。その一つひとつの試行錯誤の中にこそ、データベースの真髄が隠されています。

== サンプルコードについて

本書で実装するデータベース「nekoDB」の全ソースコードは、以下のGitHubリポジトリで公開しています。各章のコードを直接確認したり、手元で動かしたりしながら読み進めてください。

@<href>{https://github.com/Yutosaki/team-kuroneko/tree/master/src}

== 対象読者

本書は、以下のような方々に向けて書かれています。

 * @<b>{普段データベースを使っているが、裏側の仕組みを知りたい開発者}:
   ORMやSQLを使ってアプリケーションを作れるが、データベース内部で何が起きているのかはよく分からないという方。
 * @<b>{理論だけでなく、コードを書いてシステムを理解したいエンジニア}:
   「B-Tree」や「実行計画（プランナ）」といった言葉や概念は知っているが、それをどうやって実装するのか具体的なイメージが湧かない方。
 * @<b>{低レイヤやミドルウェアの開発に興味がある方}:
   自作コンパイラや自作OSのように、OSのファイルシステムとアプリケーションの間を取り持つ「ミドルウェア」を自分の手で構築してみたい方。

== 本書の進め方と各章の流れ

本書のすべての章は、「最小限の実装を作る」→「データが増えるなどの壁（課題）にぶつかる」→「解決策となる理論を学ぶ」→「実装して改善する」というサイクルで進みます。具体的には以下のロードマップで本格的なデータベースを作り上げていきます。

 * @<b>{第1章 データベースとは何か}: データベースの役割、ファイルとの違い、そしてデータベースの内部アーキテクチャについて学びます。これから構築するシステム全体の設計思想を理解します。
 * @<b>{第2章 最小のデータベース}: メモリ上だけで動作する最小構成のデータベースを実装します。対話型プログラム（REPL）を作成し、CRUD操作やハッシュテーブルによるデータ管理を実装します。しかし、データはメモリ上にしか存在せず、プログラムを終了すると消えてしまうという課題が残ります。
 * @<b>{第3章 データの永続化}: データをファイルへ保存できるようにし、シリアライズやファイル入出力を実装して永続化を実現します。しかし、更新のたびにファイル全体を書き直さなければならず、大きなデータでは非効率であるという課題に直面します。
 * @<b>{第4章 ページとスロットによるデータ配置の改善}: 「ページ」と固定長スロットを導入し、ディスクの読み書きをページ単位で行うことでI/Oを効率化します。しかしデータ量が増えると、検索が線形探索となり性能が大きく低下する問題が現れます。
 * @<b>{第5章 B-Treeによる検索の高速化と領域管理}: B-Treeインデックスを導入し、RecordIdや空き領域管理を実装して高速なCRUD処理を実現します。しかし、独自コマンドによる操作では柔軟性が低く、より一般的な問い合わせ言語が必要になります。
 * @<b>{第6章 テーブル定義とSQLの実行}: テーブル定義やカタログを導入し、Tokenizer・Parser・AST・クエリエグゼキュータを実装してSQLを実行できるようにします。しかし、この段階ではSELECTは常に全件走査となり、作成したインデックスが利用されません。
 * @<b>{第7章 プランナによる実行計画の選択}: クエリを解析して最適な実行経路を選択するプランナを実装し、必要に応じてインデックスを利用する実行計画を生成します。SQLから高速検索が可能になりますが、実行処理とストレージ処理の結合が強くなり、保守性や拡張性が課題となります。
 * @<b>{第8章 オペレータによる実行処理の分離}: Volcanoモデルに基づくオペレータを導入し、実行エンジンをストレージ層から分離します。実行計画ツリーを構築してオペレータを組み合わせることで、責務を明確にした拡張性の高いアーキテクチャへ整理します。
 * @<b>{第9章 真のデータベースに向けて}: これまで実装してきたデータベースを振り返り、商用データベースとの差や今後必要となる機能（トランザクション、並行制御、リカバリなど）を整理し、さらなる発展への道筋を示します。
本書を読み終える頃には、単なる文字列であるSQLがどのように解析され、実行計画が練られ、B-Treeのインデックスを辿り、ディスク上のページからデータが読み取られるのか——その壮大なバケツリレーの全貌を、自分の書いたコードを通して説明できるようになるでしょう。

それでは、いよいよ私たちだけのデータベースを作り始めましょう。
